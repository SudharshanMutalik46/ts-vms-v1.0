<UserControl x:Class="TSVmsDesktop.Views.AuditView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Audit Logs" FontSize="24" FontWeight="Bold" Foreground="#333" Margin="0,0,0,20"/>

        <Border Grid.Row="1" Background="White" CornerRadius="8" Padding="15" Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Filter Action:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Text="{Binding FilterAction}" Width="150" Height="30" VerticalContentAlignment="Center" Margin="0,0,20,0" Style="{StaticResource ModernInputStyle}"/>
                
                <Button Content="Search" Command="{Binding LoadEventsCommand}" Style="{StaticResource PillButtonStyle}" Width="100" Height="30"/>
                <Button Content="Export CSV" Command="{Binding ExportCommand}" Style="{StaticResource OutlineButtonStyle}" Width="100" Height="30" Margin="10,0,0,0" Foreground="#555" BorderBrush="#CCC"/>
            </StackPanel>
        </Border>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Events}" AutoGenerateColumns="False" 
                  IsReadOnly="True" HeadersVisibility="Column" 
                  Background="White" BorderThickness="0"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEE"
                  RowHeight="35">
            
            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F5F5F5"/>
                    <Setter Property="Foreground" Value="#666"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#DDD"/>
                </Style>
                <Style TargetType="DataGridCell">
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="{TemplateBinding Padding}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
                                    <ContentPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#FFF0E6"/> <!-- Light Orange tint -->
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="BorderThickness" Value="0"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Timestamp" Binding="{Binding Timestamp, StringFormat='{}{0:MM/dd/yyyy HH:mm:ss}'}" Width="160" Foreground="#555"/>
                <DataGridTextColumn Header="Actor" Binding="{Binding Actor}" Width="140" Foreground="#555"/>
                <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="180" FontWeight="SemiBold"/>
                <DataGridTextColumn Header="Resource" Binding="{Binding Resource}" Width="150" Foreground="#777"/>
                <DataGridTextColumn Header="IP Address" Binding="{Binding ClientIp}" Width="120" Foreground="#777"/>
                
                <!-- Custom Result Column with Color Trigger logic via Style is hard in DataGridTextColumn without a Converter. 
                     Using ElementStyle to achieve basic text coloring. -->
                <DataGridTextColumn Header="Result" Binding="{Binding Result}" Width="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Result}" Value="failure">
                                    <Setter Property="Foreground" Value="#D32F2F"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Result}" Value="success">
                                    <Setter Property="Foreground" Value="#388E3C"/>
                                    <Setter Property="FontWeight" Value="SemiBold"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Details" Binding="{Binding Details}" Width="*" Foreground="#555"/>
            </DataGrid.Columns>
        </DataGrid>
        
        <ProgressBar Grid.Row="2" IsIndeterminate="True" Width="200" Height="4" VerticalAlignment="Top" 
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>
    </Grid>
</UserControl>
