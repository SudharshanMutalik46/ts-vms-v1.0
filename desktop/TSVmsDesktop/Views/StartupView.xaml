<UserControl x:Class="TSVmsDesktop.Views.StartupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.Background>
            <RadialGradientBrush RadiusX="1.2" RadiusY="1.2" Center="0.5,0.45" GradientOrigin="0.5,0.45">
                <GradientStop Color="#1B1D21" Offset="0"/>
                <GradientStop Color="#131416" Offset="1"/>
            </RadialGradientBrush>
        </Grid.Background>

        <Border Background="#26282D" Width="420" Height="320" CornerRadius="16" BorderBrush="#3A3D43" BorderThickness="1">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="24" ShadowDepth="3" Opacity="0.35"/>
            </Border.Effect>

            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Margin="24">
                <Image Source="/Images/logo.png" Height="56" Margin="0,0,0,18"/>

                <TextBlock Text="{Binding StatusText}" FontSize="17" FontWeight="SemiBold"
                           Foreground="#F2F2F2" HorizontalAlignment="Center" TextAlignment="Center" TextWrapping="Wrap"/>

                <TextBlock Text="{Binding Details}" FontSize="11" Foreground="#A1A1AA"
                           Margin="0,8,0,16" TextWrapping="Wrap" MaxWidth="320" MaxHeight="42" TextTrimming="CharacterEllipsis" HorizontalAlignment="Center"/>

                <ProgressBar IsIndeterminate="True" Width="210" Height="4" Margin="0,0,0,16"
                             Foreground="{StaticResource TechnoOrange}"
                             Background="#3A3A3D"
                             Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <Button Content="Retry Connection" Command="{Binding RetryCommand}"
                        Style="{StaticResource PillButtonStyle}" Width="160" Height="40"
                        Visibility="{Binding IsRetryVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>

                <Button Content="Copy Diagnostics" Command="{Binding CopyDiagnosticsCommand}"
                        Style="{StaticResource OutlineButtonStyle}" Foreground="#D4D4D8" BorderBrush="#4A4A4F"
                        Margin="0,10,0,0" Width="160" Height="32" FontSize="11"
                        Visibility="{Binding IsRetryVisible, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
