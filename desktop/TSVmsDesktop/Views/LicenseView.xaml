<UserControl x:Class="TSVmsDesktop.Views.LicenseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="License Management" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <Border Background="White" Padding="20" CornerRadius="8">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Status: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding Status.Status}" Foreground="{Binding Status.Valid, Converter={StaticResource BooleanToVisibilityConverter}}"/>
                    </StackPanel>
                    
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Expires: " FontWeight="Bold"/>
                        <TextBlock Text="{Binding Status.Expiry}"/>
                    </StackPanel>

                    <TextBlock Text="Quotas &amp; Usage:" FontWeight="Bold" Margin="0,10,0,5"/>
                    <TextBlock Text="{Binding QuotaDisplay}" FontFamily="Consolas" Background="#F5F5F5" Padding="10"/>

                    <Button Command="{Binding ReloadCommand}" Content="Reload License" 
                            Width="150" Height="30" HorizontalAlignment="Left" Margin="0,20,0,0"/>

                    <TextBlock Text="{Binding Message}" Foreground="Blue" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
