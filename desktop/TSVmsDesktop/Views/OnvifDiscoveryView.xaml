<UserControl x:Class="TSVmsDesktop.Views.OnvifDiscoveryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <Grid Margin="20" Background="#F8F9FA">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <Button Command="{Binding CloseCommand}" Content="← Back" Margin="0,0,20,0" 
                    Background="Transparent" BorderThickness="0" Foreground="#555" 
                    FontSize="16" FontWeight="Bold" Cursor="Hand"/>
            <TextBlock Text="ONVIF Discovery" FontSize="24" FontWeight="Bold" Foreground="#333" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Control Panel -->
        <Border Grid.Row="1" Background="White" Padding="20" CornerRadius="8" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect Color="#EEE" BlurRadius="10" ShadowDepth="2" Opacity="0.5"/>
            </Border.Effect>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Button Command="{Binding StartScanCommand}" Content="Start Scan" Width="150" Height="40" Style="{StaticResource PillButtonStyle}" IsEnabled="{Binding IsScanning, Converter={StaticResource InverseBoolConverter}}"/>
                
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                    <TextBlock Text="{Binding DiscoveryStatus}" FontWeight="SemiBold" Foreground="#555" VerticalAlignment="Center"/>
                    <ProgressBar IsIndeterminate="True" Width="100" Height="4" Margin="10,0,0,0" Visibility="{Binding IsScanning, Converter={StaticResource BoolToVis}}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Results List -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Devices}" AutoGenerateColumns="False" 
                  Background="White" BorderThickness="0" HeadersVisibility="Column" RowHeight="50"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#EEE"
                  IsReadOnly="True">
            <DataGrid.Resources>
                 <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#F5F7FA"/>
                    <Setter Property="Padding" Value="10,0"/>
                    <Setter Property="Height" Value="35"/>
                    <Setter Property="FontWeight" Value="SemiBold"/>
                </Style>
            </DataGrid.Resources>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Manufacturer" Binding="{Binding Manufacturer}" Width="120"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="120"/>
                <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="120"/>
                <DataGridTextColumn Header="Codec" Binding="{Binding EncodingDisplay}" Width="80"/>
                <DataGridTextColumn Header="Resolution" Binding="{Binding ResolutionDisplay}" Width="100"/>
                <DataGridTextColumn Header="Bitrate" Binding="{Binding BitrateDisplay}" Width="80"/>
                <DataGridTemplateColumn Header="Action" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Adopt" Command="{Binding DataContext.AdoptDeviceCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}" 
                                    Width="100" Height="30" Style="{StaticResource OutlineButtonStyle}" 
                                    Foreground="#2E7D32" BorderBrush="#2E7D32"
                                    IsEnabled="{Binding IsClaimed, Converter={StaticResource InverseBoolConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
