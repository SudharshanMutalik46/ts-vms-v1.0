<UserControl x:Class="TSVmsDesktop.Views.CamerasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:TSVmsDesktop.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F5F7FA"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Height" Value="35"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Inventory" FontSize="26" FontWeight="Bold" VerticalAlignment="Center" Margin="0,0,30,0" Foreground="#333"/>
            
            <Button Command="{Binding AddCameraCommand}" Content="+ Add Camera" Style="{StaticResource PillButtonStyle}" Width="140" Height="40"/>
            
            <Button Command="{Binding OpenDiscoveryCommand}" Content="Discover" Margin="10,0,0,0" Style="{StaticResource PillButtonStyle}" Background="#2196F3" Width="120" Height="40"/>

            <Button Command="{Binding EnableSelectedCommand}" Content="Enable" Margin="20,0,0,0" Style="{StaticResource OutlineButtonStyle}" Foreground="#2E7D32" BorderBrush="#2E7D32" Height="40" Padding="15,0"/>
            
            <Button Command="{Binding DisableSelectedCommand}" Content="Disable" Margin="10,0,0,0" Style="{StaticResource OutlineButtonStyle}" Foreground="#C62828" BorderBrush="#C62828" Height="40" Padding="15,0"/>
            
            <Button Command="{Binding RefreshListCommand}" Content="Refresh" Margin="20,0,0,0" Style="{StaticResource OutlineButtonStyle}" Foreground="#555" BorderBrush="#999" Height="40" Padding="15,0"/>
        </StackPanel>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Cameras}" AutoGenerateColumns="False" 
                  CanUserAddRows="False" SelectionMode="Extended"
                  RowHeight="50" Background="White" BorderThickness="0"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F0F0F0"
                  HeadersVisibility="Column">
            <DataGrid.Resources>
                <Style TargetType="DataGridCell">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="DataGridCell">
                                <Border Padding="10,0" Background="{TemplateBinding Background}" BorderThickness="0" SnapsToDevicePixels="True">
                                    <ContentPresenter VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                             <Setter Property="Background" Value="#F0F4F8"/>
                             <Setter Property="Foreground" Value="Black"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <!-- Checkbox Column (TemplateColumn for single-click) -->
                <DataGridTemplateColumn Header="✓" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding IsSelected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                      HorizontalAlignment="Center" VerticalAlignment="Center"
                                      Style="{StaticResource ModernCheckBox}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Name Column -->
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="250" FontWeight="SemiBold"/>
                
                <!-- IP Address Column -->
                <DataGridTextColumn Header="IP Address" Binding="{Binding IpAddress}" Width="180"/>
                
                <!-- Model Column -->
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="150"/>
                
                <!-- Status Column -->
                <DataGridTemplateColumn Header="Status" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="12" Padding="10,4" HorizontalAlignment="Left">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#E8F5E9"/> 
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Offline">
                                                <Setter Property="Background" Value="#FFEBEE"/> 
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Error">
                                                <Setter Property="Background" Value="#FFF3E0"/> 
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Checking...">
                                                <Setter Property="Background" Value="#F5F7FA"/> 
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Status}" FontWeight="SemiBold" FontSize="12">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#2E7D32"/> 
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Status}" Value="Offline">
                                                    <Setter Property="Foreground" Value="#C62828"/> 
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Error">
                                                    <Setter Property="Foreground" Value="#EF6C00"/> 
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Status}" Value="Checking...">
                                                    <Setter Property="Foreground" Value="#999"/> 
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <!-- Actions Column -->
                <DataGridTemplateColumn Header="Actions" Width="*">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Details" Command="{Binding DataContext.OpenDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}" Style="{StaticResource OutlineButtonStyle}" Foreground="#555" BorderBrush="#CCC" Height="30" FontSize="12" Padding="15,0"/>
                                
                                <Button Content="X" Command="{Binding DataContext.DeleteCameraCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                        CommandParameter="{Binding}" Margin="10,0,0,0"
                                        Style="{StaticResource OutlineButtonStyle}" Foreground="#FFCDD2" BorderBrush="Transparent" Height="30" Width="30" Padding="0" 
                                        ToolTip="Delete Camera">
                                    <Button.Template>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="Transparent" CornerRadius="15">
                                                <TextBlock Text="🗑" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="#E57373"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Button.Template>
                                </Button>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
